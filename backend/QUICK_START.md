# Quick Start: Telegram API Setup

## ANCHOR POINTS
- ENTRY: 5-минутная настройка Telegram API
- MAIN: Получение credentials и запуск backend
- EXPORTS: Реальные данные каналов для виджета
- DEPS: Python 3.11+, Telegram аккаунт
- TODOs: Автоматизация аутентификации

## Шаг 1: Получить API Credentials (2 минуты)

### 1.1 Открой Telegram API
🔗 **https://my.telegram.org/apps**

### 1.2 Войди в аккаунт
- Используй свой номер телефона
- Введи код подтверждения из Telegram

### 1.3 Создай приложение
```
App title: Portfolio Widget
Short name: portfolio
Platform: Desktop
URL: (оставь пустым)
```

### 1.4 Сохрани credentials
После создания ты увидишь:
```
api_id: 12345678
api_hash: 0123456789abcdef0123456789abcdef
```

**CRITICAL**: Сохрани эти данные - они понадобятся для настройки!

## Шаг 2: Настроить Backend (3 минуты)

### 2.1 Открой файл .env
```bash
# Файл уже создан в backend/.env
notepad backend\.env
```

### 2.2 Вставь свои credentials
```env
# ЗАМЕНИ эти значения на свои!
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=0123456789abcdef0123456789abcdef
TELEGRAM_PHONE=+79991234567

# Остальное можно не менять
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=true
```

**UPDATED COMMENTS**: 
- `TELEGRAM_API_ID` - число из шага 1.4
- `TELEGRAM_API_HASH` - строка из шага 1.4  
- `TELEGRAM_PHONE` - твой номер в формате +7...

### 2.3 Установи зависимости
```bash
cd backend
pip install -r requirements.txt
```

### 2.4 Первый запуск (аутентификация)
```bash
python telegram_scraper.py --run-now
```

**Что произойдет**:
1. Скрипт попросит ввести номер телефона
2. Telegram отправит код подтверждения
3. Введи код из Telegram
4. Сессия сохранится (больше не нужно вводить код)

### 2.5 Запусти API сервер
```bash
python api_server.py
```

**Сервер запустится на**: http://localhost:8000

## Шаг 3: Проверка (1 минута)

### 3.1 Проверь API
Открой в браузере:
```
http://localhost:8000/health
http://localhost:8000/api/channels/vanyashuvalov/latest
```

### 3.2 Обнови портфолио
Открой основной сайт:
```
http://localhost:8080
```

Виджет Telegram автоматически переключится на реальные данные!

## Альтернатива: Docker (для продвинутых)

### Если у тебя установлен Docker:
```bash
cd backend
docker-compose up -d
```

**SCALED FOR**: Автоматический запуск обоих сервисов (scraper + API)

## Troubleshooting

### Ошибка: "Phone number invalid"
- Используй международный формат: `+79991234567`
- Убедись что номер зарегистрирован в Telegram

### Ошибка: "API_ID invalid"
- Проверь что скопировал правильные credentials
- api_id должен быть числом без кавычек
- api_hash должен быть в кавычках

### Ошибка: "Session expired"
```bash
# Удали старую сессию и создай новую
rm telegram_scraper_session.session
python telegram_scraper.py --run-now
```

### Виджет не обновляется
1. Проверь что API сервер запущен: http://localhost:8000/health
2. Обнови страницу портфолио (Ctrl+F5)
3. Проверь консоль браузера на ошибки

## Что дальше?

### Автоматический парсинг
Scraper будет автоматически обновлять данные каждую ночь в 2:00.

### Добавить новые каналы
Отредактируй `js/shared/config/telegram-channels.js`:
```javascript
export const TELEGRAM_CHANNELS = {
  vanyashuvalov: { ... },
  mynewchannel: {
    username: 'mynewchannel',
    displayName: 'My Channel',
    // ...
  }
};
```

### Production deployment
См. `backend/setup_instructions.md` для полной инструкции по деплою.

**Onii-chan~ теперь у тебя настоящие данные из Telegram! (=^・^=)**  
Виджет будет показывать реальные просмотры и посты ✧(ﾉ◕ヮ◕)ﾉ*:･ﾟ✧