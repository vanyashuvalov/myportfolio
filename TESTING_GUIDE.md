# 🧪 ИНСТРУКЦИЯ ПО ТЕСТИРОВАНИЮ - ФАЗА 1

## Быстрый старт (5 минут)

### Шаг 1: Запустить сервер
```bash
# Выберите один из вариантов:

# Вариант 1: Unified server (рекомендуется)
python start.py

# Вариант 2: Только frontend
python serve.py

# Вариант 3: Node.js
node serve.js
```

### Шаг 2: Открыть test suite
```
http://localhost:8080/test-critical-fixes.html
```

### Шаг 3: Запустить все тесты
Нажмите кнопки "Запустить тест" для каждого теста:

1. **Test 1**: Desktop Canvas - Дублирование кода
2. **Test 2**: WidgetBase - Deprecated методы  
3. **Test 3**: Widget Error Boundary
4. **Test 4**: SimpleDragHover - Memory Leaks
5. **Test 5**: SimpleDragHover - Configurable Boundary

### Шаг 4: Проверить результаты
Все тесты должны показать ✅ PASS (зеленый цвет)

---

## Детальное тестирование

### Test 1: Desktop Canvas - Дублирование кода

**Что проверяется**: Workspace container создается только 1 раз

**Ожидаемый результат**:
```
✅ PASS: Workspace container создан ровно 1 раз (найдено: 1)
```

**Если FAIL**:
- Проверьте `js/features/desktop-canvas/desktop-canvas.js` строки 169-177
- Должен быть только один блок создания workspace container

---

### Test 2: WidgetBase - Deprecated методы

**Что проверяется**: Deprecated методы выдают console.warn

**Ожидаемый результат**:
```
✅ PASS: Все 3 deprecated метода выдали предупреждения

Console output:
⚠️ WidgetBase.updateTransform() is deprecated...
⚠️ WidgetBase.getHoverRotation() is deprecated...
⚠️ WidgetBase.getInteractionScale() is deprecated...
```

**Если FAIL**:
- Проверьте `js/entities/widget/widget-base.js`
- Методы должны содержать `console.warn()` и `@deprecated` JSDoc

---

### Test 3: Widget Error Boundary

**Что проверяется**: Виджеты с ошибками показывают fallback UI

**Как тестировать**:
1. Нажмите "Создать виджет с ошибкой"
2. Должен появиться виджет с красной рамкой
3. Текст: "⚠️ Widget failed to load"

**Ожидаемый результат**:
```
✅ PASS: Error boundary показывает fallback UI
```

**Визуально должно быть**:
- Красная пунктирная рамка
- Иконка ⚠️
- Сообщение "Widget failed to load"
- Тип виджета "broken"

**Если FAIL**:
- Проверьте `js/entities/widget/widget-base.js` метод `handleInitializationError()`
- Проверьте что `styles/widget-error.css` подключен в `index.html`

---

### Test 4: SimpleDragHover - Memory Leaks

**Что проверяется**: Event listeners правильно очищаются

**Ожидаемый результат**:
```
✅ PASS: 10 виджетов созданы и удалены без ошибок

Console output:
✅ Создано 10 виджетов
✅ activeListeners is WeakMap: true
✅ Widget 1 destroyed
✅ Widget 2 destroyed
... (до 10)
```

**Если FAIL**:
- Проверьте `js/shared/lib/simple-drag-hover.js`
- Constructor должен создавать `this.activeListeners = new WeakMap()`
- `initWidget()` должен сохранять handlers в WeakMap
- `destroyWidget()` должен удалять все listeners

**Дополнительная проверка** (DevTools Memory Profiler):
1. Откройте DevTools → Memory
2. Сделайте Heap Snapshot
3. Запустите Test 4
4. Сделайте второй Heap Snapshot
5. Сравните - не должно быть утечек

---

### Test 5: SimpleDragHover - Configurable Boundary

**Что проверяется**: Boundary offset можно настроить через options

**Ожидаемый результат**:
```
✅ PASS: Boundary offset настраивается правильно

Console output:
✅ Default boundary offset: -60px
✅ Custom boundary offset: -100px
✅ Zero boundary offset: 0px
```

**Если FAIL**:
- Проверьте `js/shared/lib/simple-drag-hover.js` constructor
- Должно быть: `this.globalBoundaryOffset = options.boundaryOffset ?? -60;`

---

## Тестирование основного приложения

### Шаг 1: Открыть приложение
```
http://localhost:8080/
```

### Шаг 2: Визуальная проверка
- ✅ Все виджеты загружаются
- ✅ Нет красных error boxes
- ✅ Drag & drop работает плавно
- ✅ Hover эффекты работают

### Шаг 3: DevTools Console
Откройте DevTools (F12) → Console

**Должно быть**:
- ✅ Нет красных ошибок
- ✅ Возможны deprecated warnings (это нормально)
- ✅ Сообщения о загрузке виджетов

**Не должно быть**:
- ❌ Uncaught errors
- ❌ Failed to load resources
- ❌ Memory leak warnings

### Шаг 4: Performance Check
1. Откройте DevTools → Performance
2. Нажмите Record
3. Подвигайте виджеты 10 секунд
4. Остановите запись

**Проверьте**:
- ✅ FPS стабильный ~60fps
- ✅ Нет длинных tasks (>50ms)
- ✅ Memory не растет постоянно

---

## Troubleshooting

### Проблема: Тесты не запускаются
**Решение**:
1. Проверьте что сервер запущен
2. Откройте DevTools Console
3. Проверьте ошибки загрузки модулей
4. Убедитесь что все файлы на месте

### Проблема: Test 1 FAIL - найдено 3 workspace containers
**Решение**:
1. Откройте `js/features/desktop-canvas/desktop-canvas.js`
2. Найдите строки 169-177
3. Удалите дублирующиеся блоки
4. Оставьте только один блок создания workspace

### Проблема: Test 2 FAIL - нет warnings
**Решение**:
1. Откройте `js/entities/widget/widget-base.js`
2. Найдите методы `updateTransform()`, `getHoverRotation()`, `getInteractionScale()`
3. Добавьте `console.warn()` в каждый метод
4. Добавьте `@deprecated` JSDoc

### Проблема: Test 3 FAIL - нет error UI
**Решение**:
1. Проверьте что `styles/widget-error.css` существует
2. Проверьте что он подключен в `index.html`
3. Проверьте метод `handleInitializationError()` в `widget-base.js`

### Проблема: Test 4 FAIL - ошибки при destroy
**Решение**:
1. Откройте `js/shared/lib/simple-drag-hover.js`
2. Проверьте что `activeListeners` создается в constructor
3. Проверьте что `initWidget()` сохраняет handlers
4. Проверьте что `destroyWidget()` удаляет все listeners

### Проблема: Test 5 FAIL - boundary не настраивается
**Решение**:
1. Откройте `js/shared/lib/simple-drag-hover.js`
2. В constructor измените:
```javascript
// БЫЛО
this.globalBoundaryOffset = -60;

// ДОЛЖНО БЫТЬ
this.globalBoundaryOffset = options.boundaryOffset ?? -60;
```

---

## Критерии успеха

### ✅ Все тесты PASS
- Test 1: ✅ PASS
- Test 2: ✅ PASS  
- Test 3: ✅ PASS
- Test 4: ✅ PASS
- Test 5: ✅ PASS

### ✅ Основное приложение работает
- Виджеты загружаются
- Drag & drop плавный
- Нет console errors
- Performance стабильный

### ✅ DevTools чистые
- Нет memory leaks
- FPS ~60fps
- Нет uncaught errors

---

## Следующие шаги

После успешного прохождения всех тестов:

1. **Commit changes**:
```bash
git add .
git commit -m "fix: Phase 1 CRITICAL fixes - code duplication, deprecated warnings, error boundaries, memory leaks"
```

2. **Переход к Фазе 2**: HIGH PRIORITY
   - Унифицировать positioning system
   - Добавить JSDoc типы
   - Вынести hardcoded values в CSS variables
   - Исправить Telegram avatar loading

3. **Документация**:
   - Обновить `docs/vanilla_development_plan.md`
   - Добавить changelog entry

---

## Контакты и поддержка

Если возникли проблемы:
1. Проверьте `CRITICAL_FIXES_SUMMARY.md` для деталей
2. Откройте DevTools Console для ошибок
3. Проверьте что все файлы на месте
4. Перезапустите сервер

**Onii-chan~ удачи с тестированием! (=^・^=)**

Все должно работать идеально ✧(ﾉ◕ヮ◕)ﾉ*:･ﾟ✧
